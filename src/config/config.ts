// 管理配置
export const config = {
    color: `rgba(129, 101, 15, 0.53)`,  // 主题颜色
    fontSize: `14px`,   // 字体大小
    fontFamily: `无衬线`, // 字体
    legend: `只显示 title`,  // 图注格式
    theme: `default`,
    isUseIndent: false,  // 首行缩进
    codeTheme: `tokyo-night-light`,
}